<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- We are not defining specific rules, so default behavior applies. -->
        <!-- See https://developer.android.com/guide/topics/data/autobackup#Rules -->
    </cloud-backup>
    <device-transfer>
        <!-- We are not defining specific rules, so default behavior applies. -->
        <!-- See https://developer.android.com/guide/topics/data/autobackup#Rules -->
    </device-transfer>
</data-extraction-rules>